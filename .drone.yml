---
# SPDX-FileCopyrightText: Free Software Foundation Europe e.V.
#
# SPDX-License-Identifier: GPL-3.0-or-later
kind: pipeline
name: default

steps:
  # Check for REUSE compliance
  - name: reuse
    image: fsfe/reuse:latest
    commands:
      - reuse lint

  - name: quality-image
    image: docker/compose:1.29.2
    environment:
      # Docker configuration
      XDG_RUNTIME_DIR: "/run/user/1001"
      DOCKER_HOST: "unix:///run/user/1001/docker.sock"
    volumes:
      - name: dockersock
        path: /run/user/1001/docker.sock
    commands:
      - docker-compose -p reuse-api --file docker-compose.quality.yml build

  # Run pytest
  - name: quality
    image: reuse-api-quality
    pull: never
    commands:
      - make quality
      - pipenv --rm
    depends_on:
      - quality-image

  # Build and deploy Docker
  - name: deploy
    image: docker/compose:1.29.2
    commands:
      - docker-compose -p reuse-api up --build -d
    environment:
      XDG_RUNTIME_DIR: "/run/user/1001"
      DOCKER_HOST: "unix:///run/user/1001/docker.sock"
      SECRET_KEY:
        from_secret: secret_key
    volumes:
      - name: dockersock
        path: /run/user/1001/docker.sock
    when:
      branch:
        - master
      event:
        - push
        - tag
        - deployment

node:
  cont2: noris

volumes:
  - name: dockersock
    host:
      path: /run/user/1001/docker.sock
---
kind: signature
hmac: 816b42ef12ea65ad19e1759fd4aa2c2af7b1fc57cadc28c779b24c5f2a705edb

...

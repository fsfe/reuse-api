# SPDX-FileCopyrightText: 2019 Free Software Foundation Europe e.V.
#
# SPDX-License-Identifier: GPL-3.0-or-later

[requires]
python_version = "3.12" # latest supported by Alpine

[[source]]
name = "PyPi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
flask-wtf = "*" # WTForms plugin for Flask
flask-sqlalchemy = "*" # ORM wrapper interface
psycopg2-binary = "*" # Postgres implementation of the interface
email-validator = "*" # Email validation for flask-wtf
requests = "*" # HTTP library
gunicorn = "*" # WSGI HTTP server

[dev-packages]
ruff = "*" # Linter & formatter
# Testing packages
pytest-cov = "*"
requests-mock = "*"